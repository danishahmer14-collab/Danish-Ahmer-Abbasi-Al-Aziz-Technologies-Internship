# Week 1 — Day 3

## Task(s) Assigned
Topics
 Functions 
 Parameters 
 Arguments 
 Return values 
 Default parameters 
 *args and **kwargs 
 Lambda functions 
 Scope 
 Modules 
 Packages 
 Imports 
 Virtual environments 
 pip 
 requirements.txt 
 Classes and objects 
 Constructors 
 Methods 
 Inheritance basics 
 Task Projects  
 Create reusable Python modules. 
 Build a small object-oriented Python application. 

## What I Did
First I created my own Enviroment from the learning of day 1 which covered he topic of virtual enviroment then I revised the given topics and practiced them through python code explaining every concept in comments briefly then i created a reusable python module as calculator.py and imported it into main.py also i made a basic student managemt application using classes and modules which covers all the topics 

## Key Learnings
I was already familiar with functions parameters arguments Return values default parameters *args and **kwargs were what i found new *args are used to collects extra positional arguments into a tuple and **kwargs collects extra keyword arguments into a dictionary lamda function are one line function  Commonly used as a quick function passed to sorted(), map(), filter() functions in python I was familiar with the Scope of variable there are two types global and local local variable can be used inside the function they are assigned and global can be used outside the function modules are libraries of codes that can be used or imported to reuse a function like i did in my calculator.py Packages are a folder containing multiple related modules, with an __init__.py(IMP) file marking it as a package it Lets you organize larger projects. imort in ython are used to import modules or function in your .py file. I created an venv Enviroment from the learning of day 1 which covered he topic of virtual enviroment pip is used to install packages, requirement.txt is the file containing all the packages a project needs to run so anyone can create that enviroment 
to do so user can write pip install -r requirements.txt to install everyting mentioned in requirement.txt all the other topics classes obj constructor methods and inheritance are covered in my studentmanagement.py code in tasks folder 

## Files in this folder
Functions.py covers  Functions  Parameters  Arguments Return values Default parameters *args and **kwargs LamdaFunc.py covers lambda Fuctions topic scope.py covers scope  Modules Packages Imports Virtual environments pip requirements.txt topics class.py and studentmanagement.py covers  Classes and objects Constructors Methods Inheritance basics and calculator_module.py is a basic calculator module and main.py imports this to reuse its fuctions 
